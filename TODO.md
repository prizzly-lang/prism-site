# 남은 것

2026-09-21 기준, 우선순위 순. AEO/GEO 작업의 배경과 이유는 `AEO.md`에
있다 — 여기는 지금 무엇이 안 끝났는지만 모아둔 목록이다. 끝난 일의
자세한 경위는 git 히스토리와 커밋 메시지에 있으므로 여기 다시 옮기지
않는다.

**최근에 끝난 것** (자세한 기록은 각 커밋 참고):

- 커스텀 도메인 이전(`prism.adriven.co`) + 네이버·다음 검색등록 —
  `prism/docs/ROADMAP-1.4.md`①번
- 연락처 이메일을 세 곳(사이트·Play·App Store) 다 `prism@adriven.co`로
  통일, 메일함 생존 확인
- 안드로이드 1.3 출시 후 사이트 반영 (FAQ·CTA 버튼·JSON-LD) +
  JSON-LD `softwareVersion` 고정값 버그 수정
- Google Search Console — `adriven.co`가 이미 **도메인 속성**으로
  인증돼 있어서(회사 본 사이트 등록 당시 설정) 별도 속성을 새로 만들
  필요가 없었다. 도메인 속성은 모든 서브도메인·프로토콜을 자동으로
  포함하므로 `prism.adriven.co`도 이미 그 안에 있다. 「Sitemaps」에
  `https://prism.adriven.co/sitemap.xml`만 제출했다(2026-09-21,
  「제출된 사이트맵」 목록에서 확인됨). 제출 직후라 상태는 아직 "가져올
  수 없음"으로 뜬다 — 사이트 자체는 http/https 둘 다 정상 응답하니
  첫 크롤링 전 placeholder일 가능성이 높다. **며칠 뒤 상태가
  "성공"으로 바뀌는지 다시 확인할 것.**

---

## 1. 측정 체계 — ChatGPT 확인만 남음

Search Console 등록은 끝났다. 남은 건 답변 엔진 수동 확인뿐이다.

**2026-09-21 — Perplexity 확인함.** 「광고 없는 오프라인 퍼즐 게임
추천」으로 물어보니 Puzzledom·오프라인 퍼즐 모음 앱·99 Games만
나왔고 **PRISM은 아직 안 나온다.** 도메인을 옮기고 Search Console에
막 등록한 지 며칠 안 됐으니 예상된 결과다 — 색인이 어느 정도 쌓인
뒤(2주 이상 지나서) 다시 확인해봐야 의미가 있다.

**ChatGPT는 이번엔 확인 못 했다.** 게스트 모드에서 메시지 입력이
안 먹혀서(로그인 없이는 실제 채팅이 안 되는 것으로 보인다) 자동화로
못 뚫었다. 로그인된 세션에서 다시 시도하거나 사람이 직접 확인해야
한다.

분석 스크립트를 넣는 것은 "개인정보 수집 없음" 주장과 충돌하므로 하지
않기로 확정했다(`AEO.md` "하지 않기로 한 것" 참고).

## 2. 일본어·중국어 미리보기 영상 — 사람이 실제 녹화해야 진행 가능

지금 `/ja/`, `/zh-Hans/` 페이지는 영어 영상을 그대로 쓴다. 영상에 앱 UI
텍스트(「완벽」·「다음」·챕터명)가 그대로 찍혀 있어서 언어가 안 맞는
느낌을 준다. 앱이 실제로 영어를 지원하니 거짓말은 아니지만 어색하다.

**2026-09-21 정정**: 예전에 여기 "브라우저를 해당 언어로 띄워 녹화하면
된다"고 적어뒀던 건 **틀렸다.** `tools/capture-scenes.js`(prism 저장소)
자체 주석이 경고한다 — 자동화·헤드리스 브라우저에서는
`requestAnimationFrame`이 정상적으로 돌지 않아 거울이 탭 하기 전
각도에 얼어붙은 채로 캡처된다. 실제로 이 문제로 가짜 App Store
스크린샷 24장이 나온 적이 있다(스크린샷 전용 도구인데도 그랬다 —
동영상은 훨씬 더 취약하다). **그래서 세션이 자동화로 영상을 만들 수
없다.**

기존 `preview-ko.mp4`/`preview-en.mp4`가 어떻게 만들어졌는지 확인해보니
(`prism/docs/PREVIEW.md`): 실기기(USB+QuickTime `새로운 동영상 녹화`)나
iOS 시뮬레이터(`xcrun simctl io booted recordVideo`)로 2~3분 실제
플레이를 사람이 직접 녹화 → `preview/raw/{locale}/{locale}.MP4`에
저장 → `preview/cuts.json`에 컷 구간 지정 → `npx tsx
tools/make-preview.ts {locale}`로 인코딩(App Store용, 886×1920) →
그 결과물을 다시 540×818로 손으로 잘라서(원본은 `media/orig/`에)
`prism-site/media/preview-{locale}.mp4`로 쓴다. 이 과정 전체가 사람의
실제 플레이 녹화 없이는 시작이 안 된다.

**필요한 것**: `ja`, `zh-Hans` 로케일로 앱을 켜고 실제로 몇 판을 플레이하며
2~3분 녹화한 원본 영상 두 개(실기기나 시뮬레이터). 그것만 있으면 나머지
(App Store용 인코딩 + 사이트용 크롭 + `build.mjs`의 `videoOf()` 함수를
언어별로 매핑하도록 수정)는 세션이 이어서 할 수 있다. 순수 사용자 경험
개선이라 기능·매출에 영향 없음, 시간 날 때 한다.
